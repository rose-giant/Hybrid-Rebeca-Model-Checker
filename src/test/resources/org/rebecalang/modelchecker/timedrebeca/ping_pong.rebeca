reactiveclass PingActor(3) {
    knownrebecs {
        PongActor po;
    }
    statevars {
    }

    PingActor() {
        self.ping();
    }
    msgsrv ping() {
        po.pong() after(1) deadline(6);
        delay(2);
    }
}

reactiveclass PongActor(3) {
    knownrebecs {
       PingActor pi;
    }
    statevars {
    }

    PongActor() {
    }

    msgsrv pong() {
        pi.ping() after(1) deadline(5);
        delay(1);
    }
}

main {
    PingActor p1(p2):();
    PongActor p2(p1):();
}