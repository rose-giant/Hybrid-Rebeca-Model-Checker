reactiveclass PingActor(3) {
    knownrebecs {
        PongActor po;
    }
    statevars {
    }

    PingActor() {
        self.ping();
    }
    msgsrv ping() {
        delay(2);
        po.m2() deadline(5);
        delay(2);
        po.m3() deadline(5);
        self.ping() after(10) deadline(20);
    }
}

reactiveclass PongActor(3) {
    knownrebecs {
       PingActor pi;
    }
    statevars {
    }

    PongActor() {
    }

    msgsrv m2() {

    }

    msgsrv m3() {

    }
}

main {
    PingActor p1(p2):();
    PongActor p2(p1):();
}